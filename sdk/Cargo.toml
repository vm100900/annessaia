[package]
name = "annessaia-sdk"
version = "0.1.0"
edition = "2021"
description = "SDK for writing annessaia WASM apps — GPU, pixel, widget, input, storage, HTTP"

[lib]
name = "annessaia_sdk"

[profile.release]
opt-level = "s"
lto = true
strip = true
